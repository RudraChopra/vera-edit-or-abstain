# CivilComments Current Verification Report

Date: July 13, 2026

Status: `not_currently_claim_grade_verifiable`

The CivilComments-WILDS artifacts currently present in the local filesystem are
not sufficient for a claim-grade text benchmark row. The relevant receipt,
statistical report, summary CSV, per-seed CSV, metadata report, full dataset
audit, and embedding-store validation files are zero-block placeholder files.
Attempts to read the per-seed and summary CSVs hang because the files are not
materialized locally.

Claim boundary: CivilComments should remain planned or unverified until the
artifacts are regenerated or recovered and then audited.
