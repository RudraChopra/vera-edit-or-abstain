\paragraph{Natural and temporal evidence.}
Locked text, census, image, and Qwen2.5-1.5B hidden-state studies release
80/125 interfaces with 0/80 held-out and 0/500 Qwen operational violations.
A separately frozen 2023 ACS test follows two direct releases first identified
in 2021; both violate the $.40$ utility threshold empirically, and one is
familywise confirmed (error $.425$, lower bound $.405$). MOSAIC had rejected
both.

\paragraph{Power and alternatives.}
A locked real-proxy study imputes source at $.841$ balanced accuracy, then
certifies a nonconstant release with source bounds $.218/.279$ and worst-error
bound $.369$; smaller calibration folds abstain. Across 35 primary BiasBios and
ACS releases, residual shift exceeds sampling uncertainty in every certificate.
A matched $\epsilon_{\rm LDP}=.731$ randomized-response baseline releases 0/35
interfaces at the $.40$ utility contract; MOSAIC releases 35/35 and reduces
certified error by a median $.155$. Full curves and costs are in the supplement.
